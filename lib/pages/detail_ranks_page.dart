import 'package:flutter/material.dart';

class DetailRanksPage extends StatelessWidget {
  const DetailRanksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Rank Details"),
      ),
    );
  }
}
